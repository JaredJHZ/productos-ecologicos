import { IconImagePipe } from './icon-image.pipe';

describe('IconImagePipe', () => {
  it('create an instance', () => {
    const pipe = new IconImagePipe();
    expect(pipe).toBeTruthy();
  });
});
